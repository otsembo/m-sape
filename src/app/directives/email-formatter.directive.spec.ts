import { EmailFormatterDirective } from './email-formatter.directive';

describe('EmailFormatterDirective', () => {
  it('should create an instance', () => {
    const directive = new EmailFormatterDirective();
    expect(directive).toBeTruthy();
  });
});
